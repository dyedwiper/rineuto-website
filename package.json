{
  "name": "rineuto-app",
  "private": true,
  "engines": {
    "node": "16.x",
    "npm": "8.x"
  },
  "scripts": {
    "start": "cd server && npm run start",
    "start-local": "cd server && npm run start-local",
    "build": "cd client && npm install && npm run build",
    "dev": "run-p server-dev react-dev",
    "server-dev": "cd server && npm run dev",
    "react-dev": "cd client && npm run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
